//getNext求解长度为len的字符串s的next数组
//P458
void getNext(char s[], int len)
{
    int j = -1;
    next[0] = -1;
    for(int i = 1; i < len; i++)
    {
        while(j != -1 && s[i] != s[j + 1])
        {

        }
    }
}
